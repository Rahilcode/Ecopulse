<% if user_signed_in? %>
<%= render '/shared/navbar' %>
<div class="container">

<h1>Services</h1>

<div id="services" class="d-flex justify-content-center align-items-center flex-column">
  <% @services.each do |service| %>
    <%= render '/shared/service_post_card', service:service, flag: false, orderFlag: true %>
  <% end %>
</div>

</div>
<% end %>